/**
 * EXERCISE 9
 * ======================================================
 * @task
 * Make sure the BankAccount type is imported
 * Create the getClientsWithBalanceOverOneHundred() function that follows:
 * * Takes an array of bank account objects
 * * Returns an array of bank accounts with balance over $100
 * The for loop must be used
 * The function must be exported
 * Example: getClientsWithBalanceOverOneHundred(array) => [{ name: 'Name1', balance: 32, ... }, { name: 'Name2', balance: 3523, ... }]
 */

import { BankAccount } from './e5';

export function getClientsWithBalanceOverOneHundred(arr) {
  // Your code goes here...
}

// === TEST YOURSELF ===
// Once you're finished run the test with "npm run test-9"
// If the test has all tests passed, switch to the next exercise file
// If any of the tests fails, refactor the code and run the test command after you've fixed the function
